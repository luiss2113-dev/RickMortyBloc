import 'package:flutter/material.dart';

class CharactersScreen extends StatelessWidget {
  static const characterRoute = '/characters';
  const CharactersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
